name: Generate priority learning assets

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Generation disabled
        run: echo 'AI generation is disabled until GEMINI_API_KEY is replaced with the intended paid-account key.'
