name: Gemini TTS smoke test

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Smoke test disabled
        run: echo 'Gemini smoke test is disabled until GEMINI_API_KEY is replaced with the intended paid-account key.'
