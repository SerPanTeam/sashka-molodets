(() => {
  const STORE='sashka.letters.v1';
  const items=[
    {letter:'A',anchor:{de:'Apfel',ua:'яблуко',image:'./assets/generated/images/apple.png'}},
    {letter:'E',anchor:{de:'Elefant',ua:'слон',image:'./assets/generated/images/elephant.png'}},
    {letter:'I',anchor:{de:'Igel',ua:'їжак',image:null}},
    {letter:'O',anchor:{de:'Orange',ua:'апельсин',image:'./assets/generated/images/orange.png'}},
    {letter:'U',anchor:{de:'Uhu',ua:'пугач',image:null}}
  ];
  let target=null,locked=false,roundTimer=0,activeAudio=null;
  const progress=(()=>{try{return JSON.parse(localStorage.getItem(STORE))||{}}catch{return{}}})();
  const save=()=>localStorage.setItem(STORE,JSON.stringify(progress));
  const shuffle=a=>{a=[...a];for(let i=a.length-1;i>0;i--){const j=Math.floor(Math.random()*(i+1));[a[i],a[j]]=[a[j],a[i]]}return a};
  const masteredCount=()=>items.filter(x=>(progress[x.letter]?.correct||0)>=3).length;
  const visibleCount=()=>Math.min(items.length,Math.max(2,masteredCount()+2));
  const pool=()=>items.slice(0,visibleCount());
  function stopAudio(){if(activeAudio){activeAudio.pause();activeAudio.currentTime=0;activeAudio=null}if('speechSynthesis'in window)speechSynthesis.cancel()}
  function speak(text){stopAudio();if(!('speechSynthesis'in window))return;const u=new SpeechSynthesisUtterance(text);u.lang='de-DE';u.rate=.78;u.pitch=.95;const vs=speechSynthesis.getVoices();u.voice=vs.find(v=>/^de(-|_)/i.test(v.lang)&&/natural|premium|enhanced|google|microsoft/i.test(v.name))||vs.find(v=>/^de(-|_)/i.test(v.lang))||null;speechSynthesis.speak(u)}
  function makeTile(){const main=document.getElementById('main');if(!main||main.querySelector('[data-letters-entry]'))return false;const strip=main.querySelector('.category-strip');if(!strip)return false;const b=document.createElement('button');b.type='button';b.dataset.lettersEntry='1';b.className='category-button letters-entry';b.innerHTML='<span class="letters-entry-mark">A E I O U</span><span class="letters-entry-title">Buchstaben</span><span class="small">Букви · 5</span>';b.onclick=mount;strip.appendChild(b);return true}
  function install(){let tries=0;const tick=()=>{if(makeTile()||tries++>40)return;setTimeout(tick,100)};tick()}
  function mount(){clearTimeout(roundTimer);stopAudio();const main=document.getElementById('main');main.innerHTML='<section class="letters-game"><header class="letters-head"><button type="button" class="letters-back">Zurück</button><div class="letters-heading"><span>Deutsche Vokale</span><h1>Hören. Erkennen. Merken.</h1></div><button type="button" class="letters-repeat">Noch einmal</button></header><div class="letters-progress"><span>Heute</span><div class="letters-progress-track"><i></i></div><strong id="lettersLevel"></strong></div><div class="letters-prompt"><span>Hör gut zu</span><strong id="lettersPrompt"></strong></div><div id="lettersChoices" class="letters-choices"></div><div id="lettersAnchor" class="letters-anchor" aria-live="polite"></div></section>';main.querySelector('.letters-back').onclick=()=>location.reload();main.querySelector('.letters-repeat').onclick=()=>target&&speak(`${target.letter}. Finde ${target.letter}.`);next()}
  function updateProgress(){const count=visibleCount(),pct=Math.round(((count-2)/(items.length-2))*100),track=document.querySelector('.letters-progress-track i'),label=document.getElementById('lettersLevel');if(track)track.style.width=`${Math.max(0,pct)}%`;if(label)label.textContent=`${count} / ${items.length}`}
  function pickTarget(){const p=pool(),weighted=[];for(const x of p){const c=progress[x.letter]?.correct||0;for(let i=0;i<Math.max(1,4-c);i++)weighted.push(x)}return weighted[Math.floor(Math.random()*weighted.length)]}
  function next(){locked=false;target=pickTarget();updateProgress();const prompt=document.getElementById('lettersPrompt'),choices=document.getElementById('lettersChoices'),anchor=document.getElementById('lettersAnchor');if(!prompt||!choices||!anchor)return;prompt.textContent=`Finde ${target.letter}`;anchor.innerHTML='';choices.innerHTML='';for(const item of shuffle(pool())){const b=document.createElement('button');b.type='button';b.className='letter-choice';b.textContent=item.letter;b.setAttribute('aria-label',`Buchstabe ${item.letter}`);b.onclick=()=>choose(item,b);choices.appendChild(b)}roundTimer=setTimeout(()=>speak(`${target.letter}. Finde ${target.letter}.`),180)}
  function choose(item,button){if(locked)return;if(item.letter!==target.letter){const p=progress[target.letter]||{correct:0,wrong:0};p.wrong++;progress[target.letter]=p;save();button.classList.add('is-wrong');speak(`Noch einmal. Finde ${target.letter}.`);setTimeout(()=>button.classList.remove('is-wrong'),650);return}locked=true;const p=progress[target.letter]||{correct:0,wrong:0};p.correct++;progress[target.letter]=p;save();button.classList.add('is-correct');const anchor=document.getElementById('lettersAnchor');const visual=target.anchor.image?`<img src="${target.anchor.image}" alt="${target.anchor.de}">`:`<span>${target.letter}</span>`;anchor.innerHTML=`<div class="letters-anchor-picture">${visual}</div><div class="letters-anchor-copy"><strong>${target.letter} wie ${target.anchor.de}</strong><span>${target.anchor.de} · ${target.anchor.ua}</span></div>`;speak(`${target.letter}. ${target.letter} wie ${target.anchor.de}.`);roundTimer=setTimeout(next,2200)}
  if(document.readyState==='loading')addEventListener('DOMContentLoaded',install,{once:true});else install();
})();
